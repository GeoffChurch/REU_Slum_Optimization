# The passthrough Screen (run()'s default): selects nothing of its own -> None (all
# blocks), so the run falls to the region path's seed groups. Top-level `block_ids` is
# now the region grouping (threaded through PipelineSpec.block_groups), NOT a screen
# filter, so it is no longer interpolated here.
_target_: reblock.screen.identity.IdentityScreen
